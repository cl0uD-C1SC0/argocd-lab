# Script to check configurations 
# - Verify if user have Ansible, Terraform KUBECTL and AWS CLI installed.
# - Verify if user have the following permissions at AWS:
#   - Kubernetes, VPC, AWS CodeCommit, AWS CodeBuild, AWS CodePipeline, AWS ECR, AWS IAM - Create roles etc.